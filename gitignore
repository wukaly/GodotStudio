# Godot 4 import cache and editor state — regenerated on open, never commit
.godot/

# Godot 3 legacy cache, if anyone opens this in 3.x
.import/

# Exported builds
/build/
/builds/
/export/
*.exe
*.pck
*.zip
*.apk
*.aab
*.dmg
*.app

# Export presets can contain signing keys and store credentials.
# Commit export_presets.cfg deliberately if yours has no secrets in it.
export_presets.cfg

# Android build template, generated by the editor
/android/

# Mono / C# build output, if you add C# later
.mono/
data_*/
*.csproj.user
bin/
obj/

# Compiled translations
*.translation

# OS and editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/
